# 审查循环

# 审查循环

机械步骤在 `audit.py`，这份文件只写它替不了的判断。

入口：项目根的 `审查.py`，或直接 `python audit.py`。

## 立刻做这三件

```bash
python audit.py status             # 看轮次、上轮结论、待办、本轮方向
python audit.py prompt             # 拿到填好的评审提示
```

然后用 `prompt` 的输出派子 agent：

```
Agent(subagent_type="general-purpose", model=<与上轮不同>, prompt=<上面那段>)
```

**必须新起 agent，不能 fork**（fork 继承上下文＝继承盲区）。
并行派两个时用不同模型，并给每个单独的 git worktree——两个评审同时改同一目录会互相污染。

## 一轮怎么走

```
status → prompt → 派 agent → 逐条验证 → 先红后绿地修
  → audit.py mutate <每个改动文件> → audit.py check → 目测产物 → 提交 → audit.py bump
```

## 四条不能省的判断

**① 逐条验证，不要照收。** 每条先跑它给的复现方式；复现不出来的标"未证实"，不动。
它给的**修法**也要审——问题为真、解法为假的情况反复出现。

**② 先红后绿。** 先写测试并确认 FAILED，再改实现。每条检查配两侧用例：该触发 + 不该触发。

**③ 变异测试是硬门槛。** `audit.py mutate <改动文件>` 退回后必须变红，**且红的是该红的那条**。
退回后仍全绿 = 这处修复没有测试守着，补测试再继续。

**④ 停止由评审说了算，主窗口不自己判。** 出现下列任一 = 方向选错，换方向重派而不是收工：
- 评审说"无发现"但你知道还有没修完的项
- 报回来的全是打磨项（命名、注释、行长）

## 提交前

commit 正文里**每句"已修复"都要有一条刚跑过的命令支撑**。
这一环栽过三次：把错事实写进源码注释、把归因写错、声称修了实际没做——
每次都发生在"总结战果"的时候。

## 已知会踩的坑（照着查，不要重新发现）

| 症状 | 检查方式 |
|---|---|
| 批处理脚本中途失败、前面改动全丢 | 逐条 replace → 立刻语法校验 → 立刻写盘；改完 `grep -c '<旧词>'` 应为 0 |
| 判据解析"渲染结果"（刻度文本、包围盒） | 改成问对象自己是什么（`get_xscale()` / `isinstance` / 属性），或实测像素 |
| 豁免 fail-open | 任何"满足条件就跳过"的分支，问：取不到值时会怎样？应 fail-closed |
| 检查被 `except` 静默吞掉 | `audit.py check` 已自动检测「未执行/未完成」 |
| 死参数（文档登记、函数体不读） | grep 参数名，只出现在签名/docstring/文档三处就是死的 |
| 测试恰好绕开失效区 | 用**偏离**现有用例的输入复验：不同量级、类型、布局 |
| 库函数当场警告但没人看 | 跑全量时留意 `UserWarning` |
| 文档说 A、库要 B | 照文档主路径**真的走一遍**（复制模板、删掉文档让删的行、跑起来） |
| 公开 API 的字符串参数无校验 | 传一个语义上合理但非法的值（`orientation="horizontal"`），看是静默走错分支还是报错 |

## 换到别的项目

改 `<被审项目>/audit.state.json` 的 `target` / `gate`，改 `audit.py` 里 `cmd_check()` 的命令表。
两处必须重写：**gate** 要写成一句话（评审据此判达标没达标）；**目测环节**在交付物不是
图像时换成人工走一遍真实使用路径，这一环不能省。
