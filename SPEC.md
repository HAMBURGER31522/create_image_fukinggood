# figure-forge 设计规范（SPEC）

> 本文件是 skill 的设计合同：先规范，后实现。任何实现与本规范冲突时，以本规范为准；修改规范必须先改本文件。

## 0. 一句话定位

把数学建模论文的图从「正确的默认 matplotlib」抬到「期刊论证图」那一档：**每张图先有论点，构图服务论点，统计证据嵌入图内**。

## 1. 问题定义（为什么现有图平庸）

对照样本：华数杯 A 题交付物（平庸基线） vs `skills/photo` 8 张参考图（目标水准）。

| 维度 | 平庸基线 | 目标水准 |
|---|---|---|
| 论点 | 图 = 数据展示（"画了柱状图"） | 图 = 论证单元（"图内自含结论与证据"） |
| 配色 | matplotlib 默认 tab10 高饱和 | 低饱和学术色（PuOr/YlOrRd/crest、Okabe-Ito） |
| 注释 | 只有图例 | 统计注释框（RMS/CI/占比/n）+ 引线直接标注极值点 |
| 构图 | 单轴单图 | 边缘分布、放大子图、小倍数、3D+底面投影 |
| 信息密度 | 一图一变量 | 一图一论点、多证据层叠加 |
| 硬伤 | 不可通约量共用 y 轴、分组柱滥用 | 硬拒绝清单拦截 |

## 2. 核心设计原则

1. **论点合同（Figure Contract）优先**：写代码前必须先填合同——结论一句话、证据链、图种（archetype）、注释层清单。合同不完整不出图。
   - **图题数字联动硬规则**：图题/统计框/标注中的一切数字必须用 f-string 引用计算变量，禁止手写常数。手写数字会在数据变化后与图内统计矛盾，属事实错误级缺陷。
2. **硬拒绝清单**：以下构图直接拦截并给出替代：
   - 不可通约的量共用同一 y 轴（如 % 与 元 与 元/µm³）
   - 默认饼图（>3 类或需精确比较时）→ 有序水平条形 + 直接标注 / 华夫图
   - 无信息量的分组柱（类别 × 组 ≤ 8 且论点是"差异"）→ 斜率图 / 点距图（dumbbell）
   - 双 Y 轴（除非单位同族且明示）
   - rainbow / jet 色图
   - 只有均值柱没有分布信息 → 雨云图 / 带抖动散点的箱线
3. **注释层是一等公民**：每张图至少一个统计注释框（含 n、关键统计量）；关键点用引线标注（annotate + 弧形箭头），不靠读者查图例。qa.py 自动检查"图内存在带 bbox 的注释文本或引线标注"，缺失即报错。
   - **统计框三段式**：设置（网格/步长/样本量）→ 结果（RMS/极值/占比）→ 判定（是否在容差内）。只写结果的框可信度不可检验。
4. **中文数模适配**：CJK 字体自动解析（Source Han Serif SC > SimHei > Microsoft YaHei）、负号修复、字号按论文单栏/双栏印刷档（7–10.5 pt）。
   - **混排硬规则**：同一字符串禁止同时含中文与 `$mathtext$`（matplotlib 会把整串交给 mathtext 渲染，中文必然豆腐块）。混排时一律用 Unicode 数学字符（下标 ₀₁₂、上标 ⁻¹²³、希腊 φ ε η ξ σ μ Δ、符号 ± × ≤ ≥ √）；纯拉丁字符串才允许 mathtext。qa.py 自动拦截违规。
5. **出图后视觉自检闭环**：脚本出图 → agent 用 Read 打开 PNG 看 → 对照 QA 清单 → 不合格重画。

## 3. 技术栈与产物格式

- Python 3.10+，matplotlib ≥ 3.8，numpy，scipy；seaborn 仅用于 KDE/调色板辅助，不依赖其默认样式。
- 输出：PNG（预览，dpi=300）+ 可选 SVG/PDF（`pdf.fonttype=42`，文字可编辑）。
- 画布宽度：单栏 89 mm / 1.5 栏 136 mm / 双栏 183 mm（Nature 标准，兼容数模 A4 单栏 ~150 mm 档）。

## 4. 目录结构

```
figure-forge/
├── SKILL.md                 # skill 入口：工作流 + 论点合同 + 硬拒绝 + QA 清单
├── SPEC.md                  # 本文件
├── README.md                # 安装与使用
├── requirements.txt
├── core/                    # 可导入的工具层（低自由度，脚本化）
│   ├── __init__.py
│   ├── style.py             # apply_style(base_size, draft)：字体/字号/脊线/网格/导出参数
│   ├── colors.py            # 调色板：Okabe-Ito、PuOr/YlOrRd/crest 场景映射、语义色、truncate_cmap
│   ├── annotate.py          # stat_box()、callout()（引线标注）、end_label()（线端直标）
│   ├── layout.py            # marginal_grid()（边缘分布）、small_multiples()、panel_label()、inset_zoom()
│   ├── io.py                # load_table()（xlsx/CSV，自动试编码）、as_1d()（取列转数组）
│   └── qa.py                # 出图后自检：字号下限、遮挡（图例/注释框）、色图黑名单、交付宽度、硬拒绝构图、注释层、图题存在性
├── recipes/                 # 按图种分类的可运行模板（每个自带合成数据 demo）
│   ├── <按 taxonomy.md 的 P0/P1 图种一个文件>
│   └── run_all.py           # 全量回归：跑通所有 demo
├── reference/
│   ├── taxonomy.md          # 图种分类体系（agent1 调研产物，含平庸形态 vs 期刊级形态）
│   └── api.md               # 全部函数签名/返回值速查
├── tools/
│   └── contact_sheet.py     # 联络表：批量 PNG 拼网格，一次 Read 批量目测
├── gallery/                 # recipes 跑出来的示例图（入库，作视觉记忆）
└── demo/
    └── beat_baseline.py     # 门槛证明：重画 A 题平庸分组柱 → 期刊论证图
```

## 5. SKILL.md 工作流（agent 使用时）

1. **读数据与上下文** → 明确本图要支撑的论点（一句话写出）。
2. **填论点合同**：结论 / 证据链 / archetype（查 `reference/taxonomy.md`）/ 注释层清单 / 目标栏宽。
3. **过硬拒绝清单**：命中则改用替代构图。
4. **选 recipe 起步**：复制最接近的 `recipes/*.py` 改数据接口，禁止从空白 matplotlib 开始。
5. **出图** → **Read PNG 视觉自检** → 对照 QA 清单逐项过 → 不合格改到合格。
6. 交付 PNG + SVG，并把合同写进图注草稿。

## 6. QA 清单（qa.py 自动项 + 目测项）

自动项（`run_qa` 实现）。**全部为硬错**（抛 `AssertionError`，坏图不落盘），
除非标注为「警告」。需要放行时传 `allow=(码,)`，豁免会记入
`fig._ff_qa_waived` 并把文件名标成 `_QAWAIVED`；**未知的 allow 码直接抛错**。

*版面与排印*
- [ ] 最小字号：`cn` 档 ≥6.5pt / `nature` 档 ≥5pt；`nature` 档正文 ≤7pt
- [ ] **交付宽度**（tight 裁剪+补白后）属于 {89, 136, 183, 150} mm ±3mm——
      校验的是落盘文件宽度，不是裁剪前画布
- [ ] 图高 ≤170mm；整页面板数 ≤6（小倍数网格豁免）
- [ ] 中文无豆腐块（字体解析成功）、负号正常；中文与 $mathtext$ 未混排；
      字体栈无「缺 Regular 字面」家族（中文会比拉丁明显更粗）
- [ ] `apply_style()` 已调用；图题存在（结论句）
- [ ] 草稿档（dpi<250）出**警告**；文件名自动加 `_DRAFT` 防混入交付物

*硬拒绝构图*
- [ ] 未使用 jet/rainbow/hsv/turbo 色图（含 `_r` 反转变体）
- [ ] 未使用饼图（Wedge，极坐标豁免）、双 Y 轴（twinx）、共基线分组竖柱
      （`allow=("grouped_bars",)`）
- [ ] 3–6 个离散点用折线连起来 → 改点区间图（`sparse_line`）。判据是
      **轴类型**，不是刻度文本：类别轴或落在 0..n-1 小整数上 → 拦；
      对数/日期等非线性轴、等差或等比采样（连续量扫描）→ 放行；
      已有误差棒的轴放行

*遮挡与重叠*（全部硬错，`allow=("overlap",)`）
- [ ] 注释框/图例两两互压 >15%
- [ ] 带框注释压无框直标（标题类 >5%、刻度 >30%、其余 >15%）
- [ ] **图题压面板标题、面板标题互压**（>10%）
- [ ] **两个无框直标互压**（>15%）
- [ ] 图例/注释框压数据：场按面积、曲线按吞没率与绝对点数、柱顶读数位、
      像素兜底
- [ ] 轴刻度标签相邻重叠

*数值与事实*
- [ ] 数字溯源：有 `fig._ff_stats`/`sourced=` 时核对图题与注释里的数字都能
      追到计算变量（`unsourced`）；**未提供来源只警告**
- [ ] 图题/面板标题与图内注释对**同名量**给出矛盾数值（`number_conflict`）。
      只比两侧各只出现一次、能唯一配对的量名

*可读性*
- [ ] **文字对白底对比度 ≥3:1**（`text_contrast`）。语义色直接写字往往不够
      暗（Okabe-Ito 橙仅 2.25:1）；`core.annotate` 的着色点已在
      `_text_color` 里统一压暗，recipe 里裸 `ax.annotate(color=…)` 需
      自行走 `core.ink()`。深底白字（热力图注数、条内直标）自动豁免

*量纲*
- [ ] 双 Y 轴（twinx）一律硬拒绝
- [ ] **同一根 y 轴上叠不可通约的量**（线性轴、两条彩色系列取值区间不相交
      且量级差 >100×）→ 拆面板或改对数轴（`incommensurable`）。对数轴豁免

*编码与信息量*
- [ ] 分类色在三类色盲下色距 ≥0.12（`accessibility`）；已有形状/线型冗余
      编码时降为**警告**。灰度亮度差不足始终只**警告**
- [ ] 填充带（**上下界都在变**且包住某条曲线）必须有图例条目或就近直标
      （`unexplained_band`）。密度云/面积图等恒定基线填充不在此列
- [ ] 跨面板重复画同一条数据系列（`duplicate_series`）；中性色参考几何放行
- [ ] 小倍数各面板色标范围一致（`clim_mismatch`）
- [ ] 轴限利用率 <35% → 数据被压成一条线（`axis_slack`）；对数轴在 log 空间
      折算；场图与比例轴豁免。35%–60% 只**警告**
- [ ] 比例/概率轴（数据全落 [0,1] 且轴标题确有「概率/比例/率…」字样）
      轴限不得超出 [−0.10, 1.10]（`unit_axis_range`）
- [ ] 单面板墨迹 <4.5% 且面积 ≥12cm² → 近乎空白；图级平均 <13% → 信息稀薄；
      一维构图占比过高 → 换二维场/联合分布
- [ ] 数据点落在视窗外（轴限或共享轴设错，面板对读者是空白）

保存端保障（`save_figure` 实现）：
- [ ] tight bbox 宽度对称补回声明栏宽（交付宽度=声明宽度，字号契约成立）
- [ ] 内容溢出声明栏宽时打印警告
- [ ] SVG 文字非轮廓化（`svg.fonttype="none"`）、PDF/PS fonttype 42

目测项补充：
- [ ] 导出 PNG 为 300dpi（草稿档 120dpi 仅限迭代，交付前重出）

目测项（agent Read PNG 后逐项确认）：
- [ ] 一句话能说出这张图的论点，且图内证据支撑它
- [ ] 至少一个统计注释框；关键点有直接标注
- [ ] 图例不遮数据；标注不重叠（多数已自动化，见上）
- [ ] 配色低饱和、语义一致（同一对象全文同色）
- [ ] 对比"平庸基线"（如有），信息密度与论证力明显更强

## 7. 验收标准（本项目完成的定义）

**质量档位（每张出图按此判级）：**
- **合格线**：达到期刊图或模范数学建模论文（国赛/美赛 Outstanding）图的水准——构图有论点、注释层完整、配色克制、印刷可读。
- **底限（硬性）**：质量明显优于华数杯 A 题交付物的对应图（信息密度、论证力、视觉工艺三项至少两项胜出）。低于底限一律返工。

1. `recipes/` 覆盖 taxonomy 的全部 P0 图种、绝大多数 P1 图种，每个可独立运行出图到 `gallery/`，且示例图达到合格线。
2. `demo/beat_baseline.py` 重画的图经 agent2（fable5）盲评优于交付物原图（底限），并对照 `skills/photo` 参考图评估是否达到合格线。
3. 三方评审（fable5 / gpt5.6 / opus5）无 P0 级缺漏。
4. README 说明安装、目录、以 skill 方式调用的入口话术。
5. 推送至 https://github.com/HAMBURGER31522/create_image_fukinggood.git

## 8. 非目标

- 不做「图类型→适用场景」查找表（那是平庸图的生产配方）。
- 不做交互式图（plotly/bokeh）。
- 不做 Origin/R 后端，Python 单后端。
