# 评审—修复迭代工作流（可在无上下文的新窗口复现）

> 这份文件是**流程规格**，不是本仓库的设计文档（那是 `SPEC.md`）。
> 它记录 2026-08 那轮用「独立子 agent 评审 → 主窗口修复」循环把 figure-forge
> 从"看着能用"推到"经得起逐像素复核"的完整做法。**换一个项目也能照用。**
>
> 前提：主窗口能派独立子 agent；子 agent 有读写文件、跑命令、看图片的能力。

---

## 0. 一句话

**让一个不共享你上下文的 agent 反复找茬，你逐条验证后再修；每条修复都必须
配一个"退回缺陷版会变红"的测试；循环到它说"达到"为止。**

关键不在"派 agent"，在下面这些约束。没有它们，循环会退化成互相恭维。

---

## 1. 为什么要独立子 agent，而不是自己复查

自己复查会**沿用自己的错误前提**。实测本项目 13 轮里，有 4 次是"我以为修好
了、测试也绿了、但机制根本没生效"——每次都因为我写的测试用例恰好落在失效区
外面（用 `[0,1]` 的概率数据测一个在其他量级失效的判据、用线性轴测一个只在
log 轴失效的分支）。子 agent 不知道我的思路，会去构造我想不到的输入。

**必须是"新起 agent"而不是 fork**：fork 继承上下文，等于把我的盲区一起继承。

---

## 2. 单轮循环的六步

### 步骤 1 — 主窗口交出可复现的当前状态

提交所有改动，确认工作树干净。子 agent 的第一件事是 `git log -1` + `git status`，
**工作树不干净它就无法区分"你的改动"和"半成品"**。

### 步骤 2 — 写评审提示（这一步决定这轮的质量）

必须包含：

| 要素 | 为什么 |
|---|---|
| **上一轮的问题清单**（编号 + 一句话 + 复现方式） | 让它逐条核验，而不是重新漫游 |
| **主窗口声称修了什么** | 它才能验"声称 vs 实际" |
| **本轮的重点方向**（1–2 个） | 不给方向它会平均用力，抓不到深层问题 |
| **明确的判定口径** | 见步骤 4 |
| **"查完没问题就明说无发现，不要为凑数编造"** | 否则后期它会拿打磨项充数 |
| **"若确实达标就直接说达到"** | 否则它会为了显得尽职而永远不说达标 |

重点方向要**每轮换**。本项目的有效序列：
1. 正确性 bug → 2. 测试质量 → 3. 新引入的回归 → 4. 单点检查的边界压测
→ 5. **用户视角实跑**（最有效）→ 6. 全局收尾（API 一致性、文档矛盾、目测全部产物）

> **第 5 项是转折点。** 前 10 轮都在追单点缺陷，第 11 轮改成"假装第一次用，
> 只读入口文档照骨架写图"，立刻撞出两处十轮没人碰到的硬拒。

### 步骤 3 — 主窗口逐条验证，**不要全盘照收**

子 agent 会报错。本项目实测约 15% 的发现是它自己搞错的（测试夹具写错、
把正当构图当缺陷）。**每条都要自己跑一遍复现**再动手。

同时**保留否决权**：它建议的修法未必对。本项目有两次我改了它的方案——
- 它建议"自动派生统计量兜底"，我实测发现会把 RMS/R²/AUC 这类合法计算量全
  判成手写常数，误报会淹没真问题，改成了一条精确的"图题与注释矛盾"检查；
- 它建议把某条检查降级为 WARN，我改成收窄判据，保留检出力。

### 步骤 4 — 判定口径要随轮次收窄

前期："有没有正确性缺陷"。
后期（约第 10 轮起）：**"交付给用户的东西有没有会让人拿到错误结果或被无理由
硬拒的缺陷，不看内部整洁度"**。

不收窄的话，后期会耗在"续行残留""注释错位"这类打磨项上，而真问题已经稀疏。

### 步骤 5 — 修复的三条硬规矩

**① 先写会红的测试，再改实现。**
每条修复都要有一个测试，在**退回缺陷版时变红**。

**② 改完做变异测试。**
把修复逐个退回，确认对应测试各自变红、**且红的是该红的那条**。本项目靠这一
步抓到过"三条断言里有一条是死的——它声称钉住的那件事发生时永远不触发"。

**③ 每条检查配"该触发 + 不该触发"两侧用例。**
只写正例会让判据无限放宽，只写反例会让它无限收紧。

### 步骤 6 — 全量回归 + 目测

```bash
pytest tests/ -q                 # 单元
python recipes/run_all.py        # 全量产物
```
再**用眼睛看图**。本项目多次出现"QA 全绿但图是坏的"：极坐标缺整条径向刻度、
色标刻度压邻格标题、白字压浅色段。**自动检查只是底线，不是上限。**

---

## 3. 六条反复救命的具体技巧

### 3.1 静默失效是最危险的失败模式

`except` 会把"这条检查已经失效"吞成一行 note。加一条结构性测试：

```python
def test_no_check_is_silently_skipped(capsys):
    run_qa(normal_figure, strict=False)
    assert not [l for l in capsys.readouterr().out.splitlines() if "未执行" in l]
```

本项目靠它抓到：函数体内一句 `import re` 把 `re` 变成局部变量、遮蔽模块级的，
导致整块检查落进 except 静默消失。

### 3.2 批处理脚本失败会静默

用脚本批量改多个文件时，**逐条落盘**（每条 `replace` 后立刻 `ast.parse` + 写盘），
不要攒到最后一次写。本项目有一次脚本在中途撞 SyntaxError，前面的改动全部丢失，
而我没复查就把"已修复"写进了 commit——**交付图里的错话又留了一轮**。

改完立刻验：`grep -c '<被替换的词>' <文件>` 应为 0。

### 3.3 不要在 commit message 里写没验证的话

本项目三次同类问题，**每次都发生在"总结战果"的环节**：
- 把错的事实写进源码注释（"次刻度取不到任何东西"——实测有 22 条）
- 把归因写错（"极坐标+mpl 的性质"——实为本库自己的 `axes.axisbelow=True`）
- 声称修了实际没做（脚本静默失效）

**规矩：commit 正文里每一句"已修复"，都要有一条刚跑过的命令支撑。**

### 3.4 判据不要解析"渲染结果"

本项目踩过两次：
- 用 `float()` 解析刻度文本判断是不是类别轴 → log 轴的 `$\mathdefault{10^{0}}$`、
  日期、千分位全被误判
- 用几何包围盒冒充"深色背景" → `ContourSet.get_window_extent()` 返回
  `Bbox(inf,inf,-inf,-inf)`，而 `Bbox.intersection` 与退化框求交**返回原框本身**

**问对象自己是什么**（`ax.get_xscale()`、`isinstance(converter, ...)`、
`artist.filled`），或**实测真实像素**（把文字藏起来渲一次再采样）。

### 3.5 豁免要 fail-closed

任何"满足条件就跳过检查"的分支，都要问：**条件取不到值时会怎样？**

本项目的描边豁免踩了三轮：
1. `getattr(e, "_gc", {}).get("foreground", "white")` → 属性缺失默认成白色
   → `pe.Normal()` 一行绕过一条硬拒检查
2. 改成"把描边色当背景"→ 只看键存不存在，`linewidth=0.1` 的发丝描边照样豁免
3. 最终：**按覆盖率加权** `bg = w·stroke + (1-w)·measured`，w 随 lw/字号与
   alpha 单调，退化取值自然落到 w≈0

### 3.6 死参数与死代码

登记在文档里但函数体从不读取的参数（本项目的 `expand_axes`）、条件恒真的
`if True:`、精确类名比对在新版库里失配（`fill_between` 在 mpl 3.10 改名为
`FillBetweenPolyCollection`，导致一条检查**连警告都没打过**）。

查法：`grep` 参数名，若只出现在签名/docstring/文档三处就是死的。

---

## 4. 何时停

**由子 agent 说"达到"，不由主窗口自己判。**

收敛信号（本项目实测）：
- 每轮必修项数：4 → 3 → 1 → 2 → 2 → 1 → …
- 后期发现开始集中在**同一处代码的逐次收口**，而不是新的结构性缺陷
- 出现"产品输出无错误，剩下的全是测试/文档问题"这类结论

**警惕假收敛**：若某轮子 agent 说"无发现"但你知道有没修完的项，说明提示里
的重点方向没换、或判定口径写松了。

---

## 5. 新窗口的启动模板

````
你是独立评审，第 N 轮。审查 <绝对路径>（HEAD = <sha>，工作树干净）。

第 N-1 轮给出"未达 99%"，必修 M 条：
- <编号> <一句话> <复现方式>
主窗口声称已修完，另修了 <...>。

**验证，并给出是否达到 99% 的判断。**

不要客气，也不要为了凑数编造问题；某类查完没问题就明说"查过，无发现"。
**如果确实达到 99%，请直接说"达到"** —— 不要为了显得尽职而硬凑；
反过来若仍有实质缺陷也不要放水。

判断标准：**交付给用户的东西有没有会让人拿到错误结果或被无理由硬拒的缺陷**，
不看内部整洁度。

A. 逐条核验上述 M 条（已修/部分修/未修/修错了，附可复现验证）。
   **重点验 <本轮方向>**：<2–3 个具体问题>
B. 找本轮新引入的问题。改动集中在 <文件清单>。
C. 实测：<测试命令> / <全量产物命令>；目测至少 N 张产物。

输出：1) 核验表 2) 新问题（无就写"无"）3) 明确判断：是否达到 99%？
````

---

## 6. 这套流程在本项目的实际产出

13 轮，从"25 张图能出、QA 全绿"到：

- **131 条测试**，每条配"该触发 + 不该触发"两侧，全部经变异测试确认有判别力
- **QA 从 ~12 条检查扩到 ~30 条**，其中 9 条是"规范里写了但从没执行"的
- 修掉 **3 处死代码**（精确类名比对在新版 mpl 失配、`if True:`、死参数）
- 修掉 **4 处"我以为修好了但机制没生效"**
- 修掉 **2 处我自己引入的回归**（把合法图形硬拒）
- 修掉 **14 处交付物里的事实错误**（图题数字与图内不符、docstring 常数与实图矛盾、
  图上写"红"画的是橙）

**最有价值的两轮**：第 11 轮（换成用户视角实跑）和第 8 轮（要求子 agent 独立
重做变异测试，而不是相信我的结论）。
